import "std/io";

struct String
{
	char[] str;
	int test;

	func int Test()
	{
		return test;
	}

	func char CharAt(int i)
	{
		return str[i];
	}

	func int Length()
	{
		return str.Length();
	}

	func bool Equals(String other)
	{
		PrintLn('L');
		PrintLn(Length());
		PrintLn(other.Length());
		
		if(Length() != other.Length())
		{
			PrintLn("Length() != other.Length()")
			return false;
		}

		//loop(var i = 0; i <)
		return false;
	}
};
