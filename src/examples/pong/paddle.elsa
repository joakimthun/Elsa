struct Paddle
{
	int x;
	int y;
	int width;
	int height;
	int vel_y;
};

fn Paddle create_paddle(int x, int y, int width, int height)
{
	var p = new Paddle;
	p.x = x;
	p.y = y;
	p.width = width;
	p.height = height;

	return p;
}