use "std/io";
use "std/assert";
use "std/window";

fn main() {
	var w = CreateWindowHandle("Test", 800, 600);
	OpenWindowHandle(w);
	
	while(true) {
		var msg = PeekMessage();
		while(msg != -1) {
			PrintLn(msg);
			msg = PeekMessage();
		}
	}
}