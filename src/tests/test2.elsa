use "std/io";
use "std/assert";

fn main() {
	var f = OpenFile("examples\bouncy\assets\asset.bmp", FileMode::Read);

	var buffer = new byte[];
	var read = ReadFile(f, buffer, 1024);
	PrintLn(buffer[0]);
	PrintLn(read);
}